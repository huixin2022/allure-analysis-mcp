[project]
name = "server"
version = "0.3.0"
description = "MCP server for Allure test report analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.5.0",
]
