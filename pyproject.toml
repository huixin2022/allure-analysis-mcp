[project]
name = "server"
version = "0.2.0"
description = "MCP server for Allure test report analysis with Jira integration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.5.0",
    "requests>=2.31.0",
]
